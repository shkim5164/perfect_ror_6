<pre>
6.1.1 params 변수
1. form 태그로 넘기기
2. 라우트로 넘기기
3. url끝에 ? 이 붙는 형식
------------------------------------------------------------------------------------
ex)
1. 하나만 넘기기
https://perfect-ror-6-shkim5164.c9users.io/home/create?id=177

2. 두개 이상 넘기기
https://perfect-ror-6-shkim5164.c9users.io/home/create?id=177&gg=888&hh=898

3. 배열로 넘기기
https://perfect-ror-6-shkim5164.c9users.io/home/create?id[]=177&id[]=888&id[]=898

4. 해쉬로 넘기기 등
------------------------------------------------------------------------------------

6.1.2 strong parameters

1. params.require(model).permit(속성, ...)
- 이거를 이용하면 사용자가 정의할 수 있는 속성 값을 제한할 수 있다.
=> 악의를 가진 사용자가 모델 및 데이터베이스를 조작하는 것을 방지

6.2 응답

6.2.1 render 메서드

- 라우트에서 'home#index' 가 호출하는 것은 기본값이 index view이다.
- 그러나 다른 템플릿을 호출하고 싶다면 controller에 render를 써준다.

1. render action: '다른 액션'

2. render template: 'posts/1'

3. 생략형
- render 'example'
- render 'posts/1'

4. 문자열 출력
- render text: '내 문자열'

5. 인라인 템플릿 출력
- render inline : '<%= @post.title %>'

6. 빈내용 출력과 상태코드로의 연결

상태 코그 표 : http://dailybeat.io/51

6-1. render nothing: true, status : 코드

6-2. head 코드 or 심볼

7. redirect_to
- 뒤에 url, 같은 컨트롤러의 액션, 다른 컨트롤러, 액션, 생성된 뷰 헬퍼, 이전 페이지

8. before_action :strat_logger, only: [:index, :create]
9. after_action :end_logger, except: :index
10. around_action :aroud_logger

</pre>

